{
  "name": "biblioteka-extension",
  "private": true,
  "version": "1.0.0",
  "description": "Browser extension for Save to Biblioteka",
  "scripts": {
    "build": "vite build --config vite.config.extension.ts",
    "dev": "vite build --config vite.config.extension.ts --watch",
    "check": "tsc --noEmit --project tsconfig.extension.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.318",
    "typescript": "^5.7.3",
    "vite": "^6.3.2"
  }
}
