PORT=8080
DATABASE_URL=
POSTGRES_PASSWORD=
REDIS_URL=redis://localhost:6379
JWT_SECRET=your_jwt_secret_here
TELEMETRY_ENABLED=false
TELEMETRY_ENDPOINT=
SECURE_COOKIES=false
DISABLE_SIGNUP=false
# Bootstrap admin user — only used when the users table is empty on first startup.
# Remove after the initial admin account has been created.
# INITIAL_ADMIN_EMAIL=admin@example.com
# INITIAL_ADMIN_PASSWORD=changeme
# INITIAL_ADMIN_NAME=Admin
LOG_LEVEL=info
LOG_FORMAT=json
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=http://localhost:8080/api/auth/oidc/callback
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS=starttls
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_ORIGINS=http://localhost:8080
WEBAUTHN_RP_NAME=Biblioteka