// Backward-compat shim: types have been split into domain modules under ./types/.
// Consumers may import directly from "./types/<domain>" or continue using this barrel.
export * from "./types/index";
